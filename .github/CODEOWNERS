# Both founders must approve any change to how the PKI is defined or run.
# This covers workflows, shared helpers, policy config, partner list, and
# local setup scripts (tampering with a setup script could exfiltrate a
# passphrase via the `read -rs` prompt before `gh secret set` runs).

.github/   @felixboehm @Nantero1
scripts/   @felixboehm @Nantero1
